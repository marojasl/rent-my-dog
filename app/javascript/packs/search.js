// const search = document.getElementById("search");
// search.addEventListener("submit", (event) => {
  //take the values from slider and store in variable
  //change the value of the hidden field tag before submitted
// })
